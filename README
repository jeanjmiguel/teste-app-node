# DESCRITIVO

Efetuando o teste da aplicacao


# Como rodar

É necessário ter o docker instalado.

### Rode os comandos:

```sh
$ docker build -t seunome/docker-teste-node-app .
$ docker run -p 8080:8080 -d seunome/docker-teste-node-app
```

### Acesse sua aplicação:

Em seu navegador vá até `localhost:8080`
